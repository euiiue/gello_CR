# ServoJ GELLO 控制实验副本

## 边界与启动

- 当前可用版保持不动：/home/ace/cyf/TEST_PY12_SERVOJ_TEST
- 原始工程保持不动：/home/ace/cyf/TEST_PY12
- 实验副本：/home/ace/cyf/TEST_PY12_MOVEJ_LOW_LATENCY_TEST
- 当前使用 GELLO J1–J6 相对关节映射、NRC 7000 ServoJ、J7 控制 O6；不经过 ROS。
- 本轮没有连接实机、上电、清错、切换真实控制柜模式或发送运动指令。

只运行一份控制程序。正常退出占用 NRC 6001/7000、GELLO 和 O6 的其他程序后，启动：

```bash
cd /home/ace/cyf/TEST_PY12_MOVEJ_LOW_LATENCY_TEST
./run_test_inexbot.sh
```

窗口标题和启动日志均带“ServoJ 实验副本”，启动本身不连接硬件。

## 当前配置

| 参数 | 初始值 |
| --- | --- |
| 控制模式 | ServoJ；NRC 7000 |
| ServoJ vmax / amax / jmax | 50 / 100 / 200 |
| GELLO 读取与 ServoJ 发送 | 100 Hz |
| MoveJ 对照参数 | 保留在配置中，但当前 ServoJ 跟随不使用 |
| GELLO J1–J6 全局相对倍率 | 1.20×；可调 0.50–1.50×，J7/O6 不缩放 |
| CR3A 关节锁定 | 参数维护中勾选 J1–J6；当前全部解锁，包含 J5 |

当前配置将 `motion_mode` 固定为 `servoj`，并将 GELLO 反馈频率设为 100 Hz。ServoJ 参数不是任意现场安全性的保证；硬件限位、工作区、负载及急停仍需操作者确认。

操作入口：“参数维护 → GELLO 从臂跟随参数（ServoJ）”。先停止跟随、处理当前 Episode，再修改参数，点击“保存并应用参数”；重新开始跟随后生效。

勾选“锁定 CR3A 关节”中的某个 J1–J6 后，该关节固定为开始跟随时的 CR3A 姿态，GELLO 对应关节不再参与目标或速度映射；取消勾选即可恢复控制。当前配置 `locked_joints=[]`，J5 已解锁。

## 数采工作台

- 工作台顶部提供“一键回 HOME”：跟随中可直接接替 GELLO，先停止 ServoJ，再执行已保存 HOME；空闲或故障状态也可使用。若正在录制，Episode 保持录制并写入 HOME 回位过程；HOME 完成后由操作者手动结束并保存整段。
- LeRobot Episode：`开始 Episode` → `结束录制 · 待审` → 选择结果 → `保存当前`；保存完成后再次点击 `开始 Episode`，继续保存下一条 Episode。同一数据集会话中的已保存 Episode 累计计数，不覆盖前一条。
- 若抖动或相机问题使当前 Episode 已产生采样错误但仍有帧，`保存当前` 按钮仍可用；未选择结果时按失败片段保存，不能将带错误的 Episode 标为成功。
- 动作轨迹：数采工作台提供轨迹 1、2、3 的独立 `录制`、`停止并保存` 和 `回放`，记录 CR5 关节与 O6 同步动作；录制可在 GELLO 跟随期间开始。
- LeRobot Episode 的保存/丢弃和动作轨迹文件是两套独立数据，前者写入 LeRobot 数据集，后者写入配置中的 `joint_replay*.json` 文件。

### ServoJ 发送频率

ServoJ 跟随按 GELLO 反馈频率连续向 NRC 7000 提交目标；当前配置为 100 Hz。界面显示实际 SDK 提交计数和样本年龄。ServoJ 模式不使用 MoveJ 的静止重复目标抑制，因为即使目标不变也需要保持连续指令流。

### HOME 点击行为

- “保存当前为 HOME”点击后立即覆盖 HOME，不再弹出覆盖确认；仍要求跟随已停止且当前 Episode 已处理。
- “CR3A / O6 回 HOME”点击后立即执行原有回位，不再弹出运动确认；原有 HOME 存在、状态、使能、反馈和运动安全检查仍保留。
- 两个按钮都不会因为取消了弹窗而自动运行；只有操作者点击时执行。

### J1–J6 映射倍率

完整期望目标按以下方式生成：`CR3A_start + joint_scale × GELLO_relative_delta`。倍率仅作用于 J1–J6；GELLO J7 到 O6 的开合比例不变。倍率同步进入主臂速度保护，但不会替代 CR3A 绝对关节限位保护。

## 当前 ServoJ 路径

1. 启动跟随前调用 `enable_servo_position_motion_control(7000, True)`，随后打开 ServoJ 通道。
2. GELLO J1–J6 目标通过 7000 连续发送；J7 不再线性控制 O6 六个电机，而是触发两个配置动作：张开端触发“张开手”，闭合端触发“抓取”。
3. ServoJ 不执行 MoveJ 的 4098 忙等待和静止去重逻辑。
4. SDK 返回成功只计为提交，不计为实际运动确认。
5. J7 动作只在“张开手/抓取”状态变化时下发一次，不会在每个 ServoJ 周期重复触发。
6. 结束跟随时关闭 ServoJ 并保持 O6；如果控制柜报警或反馈失联，按现有安全逻辑停止。

## 保留的保护

- 原有模式准备、上/下电、停止、位置归一化、直接 MoveJ 和 ServoJ 方法均与可用版一致；没有新增模式切换指令。
- GELLO/O6 读取实现、关节标定、J7 映射、HOME/预设点、硬件地址未改变。
- 未使能、主臂/O6 掉线、原有速度/步长/跟踪误差检查仍保留。
- 短段额外检查“完整期望与实际位置的差”。它不能用小的已提交目标误差掩盖大的主从滞后，也不会放宽原有安全阈值。
- 单段上限是相对一次反馈的目标差上限，不是硬件关节绝对限位、碰撞检查、实际运动距离或时延保证。

此前日志出现过 J4 超限 9745。测试前应在现场确认报警原因已处理、当前状态正常且所选关节远离限位；不能靠放宽保护或反复上电继续试验。

## 日志和指标

日志文件：

```text
/home/ace/cyf/TEST_PY12_MOVEJ_LOW_LATENCY_TEST/logs/gello_low_latency_test.log
```

界面显示 ServoJ 软件时序、实际提交频率、GELLO 样本年龄和目标反馈差。

| 字段 | 含义 |
| --- | --- |
| GELLO_age | 进入本地发送步骤时，GELLO 软件样本的年龄 |
| read | 同步读取 CR3A 关节反馈的调用耗时 |
| dispatch | 7000 ServoJ 发送调用耗时，不等于纯网络耗时 |
| SDK_submit | 最近统计窗口的实际 SDK 提交频率，不是日志行频率或执行确认频率 |
| span | 本次目标相对本次关节反馈的最大六轴差值 |
| desired_err | 完整期望目标相对实际反馈的最大六轴误差 |
| phase | ServoJ 通常为 submitted；异常时以安全停止日志为准 |

这些指标不等于物理编码器时间戳或机器人端到端响应时间。读取/发送明显耗时时，可能还包括 SDK 阻塞或状态轮询的锁竞争，需要据日志继续区分。

## 人工对照验收

1. 先确认工作区、急停、报警及实际关节位置；仅按之前成功的现场设置操作，不根据本实验擅自切换控制模式。
2. 首先不录制正式数据，选择远离限位的一个关节，缓慢做很小动作，再停止主臂，观察从臂追赶。
3. 在实验副本内分别关闭和开启短段开关，使用相同速度、加减速度、周期和尽量相同的动作做对照。每次切换前先停止跟随。
4. 比较完整期望差是否更小、停止主臂后追赶是否更短，以及 read/dispatch/busy_wait 是否下降。可用视频辅助判断实际响应；不要把一秒一行的日志间隔当作精确延迟。
5. 若完整期望差持续增大、出现反向回摆、异常抖动、超限或安全停止，立即停止并保留日志。不要通过提高安全阈值让实验继续。

短段可能缩短对旧目标的追赶，也可能因频繁加减速而变慢；如果固定的 SDK/控制柜作业开销占主导，短段未必改善。不承诺达到实时 ServoJ 的跟踪效果。

## 数据与回退

- LeRobot state 18D / action 12D 格式和 recorder 未改变；action 依据最后实际向 SDK 提交的 ServoJ 目标。
- ServoJ 连续提交，因此采集新鲜度依据实际 SDK 提交时间；最近有效目标时间仍用于界面观测，不替代真实提交时间。
- 采集前和采集中同时检查短段目标误差、完整期望误差、反馈/动作新鲜度，失败仍保留 Episode 错误信息。
- 实验数据默认单独保存到 /home/ace/datasets/cr5_o6_low_latency_test；不要与合格训练数据混用。
- 回退无需覆盖文件：退出实验副本后，继续使用 /home/ace/cyf/TEST_PY12_SERVOJ_TEST/run_test_inexbot.sh。

## 已完成的软件验证

- 离线回归测试覆盖 MoveJ 对照路径和 ServoJ 连续发送路径；测试未打开机器人或串口。
- Python 编译和启动脚本语法检查通过。
- 对照源码确认原有控制模式、上电/停止、GELLO/O6 实现及 LeRobot recorder 未改。
- 测试覆盖开关关闭时的完整目标、六轴同比例缩短和第七值保持、小目标精确收敛、忙后同周期恢复、读取后的新鲜输入、完整误差保护、静止去重、未到位重试、两类时间戳、跟随期轻量轮询及采集 action 来源。
- Qt 离屏界面预览完成，预览使用明确标注的模拟数据。
- 独立源码、Python 与 NRC 模块加载路径已核验。

实机低延迟效果：尚未验收。测试代码不能替代现场验收。
